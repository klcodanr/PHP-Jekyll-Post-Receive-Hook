{
  time_limit: 0,
  jekyll_command: "/usr/bin/jekyll",
  projects_root: "/opt/scratch",
  servers: {
    'https://github.com/user/repo':{
      id: repo,
      jekyll_params: 'build -d /var/www/vhosts/repo'
    }
  }
}
